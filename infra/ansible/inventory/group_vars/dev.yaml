---
python_version: "3.12.*"

infisical_cli_image: "docker.io/infisical/cli:0.43.100"

pixi_path:
  pixi_home: "{{ ansible_facts.env.HOME }}/.pixi"
  pixi_bin_path: "{{ ansible_facts.env.HOME }}/.pixi/bin"
