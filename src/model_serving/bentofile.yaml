#src/model_serving/bentofile.yaml

service: "service:TextClassifier"

include:
  - "src/"
  - "bento_requirements.txt"

python:
  requirements_txt: "bento_requirements.txt"

docker:
  base_image: "python:3.12-slim"   # ou un autre environnement

#build_args:
#  run_id: "undefined"

labels:
  project: "rakuten"
  module: "text-serving"
#  built_from_run: "{{ run_id }}"

#description: "Built with model from run {{ run_id }}"